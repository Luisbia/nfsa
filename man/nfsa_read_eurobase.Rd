% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nfsa_read_eurobase.R
\name{nfsa_read_eurobase}
\alias{nfsa_read_eurobase}
\title{Read and parse Eurobase flat files for NFSA data}
\usage{
nfsa_read_eurobase(
  type = "nasq_10_ki_partial_Y",
  time_min = lubridate::today(),
  time_max = lubridate::today(),
  path = "I:/econ/NFSA/ref/sent2eurobase/"
)
}
\arguments{
\item{type}{\code{character(1)}. Pattern used to match filenames in \code{path}.
Matched as \verb{*<type>*.zip}.
Defaults to \code{"nasq_10_ki_partial_Y"}. Other options are \code{"nasq_10_ki_partial_N"},
\code{"nasq_10_nf_tr_N"},\code{"nasq_10_nf_tr_Y"}, \code{"AT_nasa_10_nf_tr_N"}....}

\item{time_min}{\code{Date} or \code{character(1)} coercible to a Date via
\code{\link[lubridate:as_date]{lubridate::as_date()}}. Only files with a modification date on or after
this value are parsed. Defaults to \code{\link[lubridate:now]{lubridate::today()}}.}

\item{time_max}{\code{Date} or \code{character(1)} coercible to a Date via
\code{\link[lubridate:as_date]{lubridate::as_date()}}. Only files with a modification date on or before
this value are parsed. Defaults to \code{\link[lubridate:now]{lubridate::today()}}.}

\item{path}{\code{character(1)}. Directory to scan for matching files.
Defaults to \code{"I:/econ/NFSA/ref/sent2eurobase/"}.}
}
\value{
A tibble with one row per data observation, containing:
\describe{
\item{\code{file}}{Full path of the source file.}
\item{\code{sent}}{File modification time (\code{POSIXct}).}
\item{\code{TIME}, \code{GEO}, \code{NA_ITEM}, ...}{Dimension columns parsed from the
\verb{FIELDS=} header line of each file. Column names vary by file type.}
\item{\code{VALUE}}{Numeric observation value.}
\item{\code{FLAG}}{Optional quality flag parsed from a \code{~}-delimited suffix
on the value field (e.g. \code{48.44~p}); \code{NA} if absent.}
}
Returns an empty tibble (with a warning) if no matching files are found.
}
\description{
Scans a directory for Eurobase flat files (plain or zipped) matching a given
type, filters them by file modification date, and parses them into a single
tidy tibble. Files that are malformed or contain no data rows are silently
skipped.
}
\details{
\subsection{File format}{

The function expects Eurobase flat files with the following structure:

\if{html}{\out{<div class="sourceCode">}}\preformatted{FLAT_FILE=STANDARD
...
UPDATE_MODE=RECORDS
FIELDS=TIME,GEO,NA_ITEM,...
1999Q01 IT B2G_B3G_RAT_S11 ...  48.44
...
END_OF_FLAT_FILE
}\if{html}{\out{</div>}}

Data rows are read from the line after the last \verb{FIELDS=} header to the line
before \code{END_OF_FLAT_FILE}. If \code{UPDATE_MODE=RECORDS} is present, the \verb{FIELDS=}
line immediately following it is used; otherwise the last \verb{FIELDS=} line in
the file is used as a fallback.
}

\subsection{Zip support}{

Files with a \code{.zip} extension are handled transparently via \code{\link[base:connections]{base::unz()}}.
If the archive contains multiple files, the first one is used.
}

\subsection{Error handling}{

Files that cannot be parsed (e.g. missing markers, empty data sections) are
skipped via \code{\link[purrr:possibly]{purrr::possibly()}} rather than aborting the entire run.
}
}
\examples{
\dontrun{
# Parse files sent today (default)
df <- nfsa_read_eurobase()

# Parse files of a specific type sent on a given day
df <- nfsa_read_eurobase(
  type     = "nasq_10_ki_partial_Y",
  time_min = "2026-04-09",
  time_max = "2026-04-09"
)
}


}
