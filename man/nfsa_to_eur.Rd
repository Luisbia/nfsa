% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nfsa_to_eur.R
\name{nfsa_to_eur}
\alias{nfsa_to_eur}
\title{Convert National Currency to Euro}
\usage{
nfsa_to_eur(data, freq = "Q")
}
\arguments{
\item{data}{A data frame containing data in national currencies, with columns \code{ref_area} (country code) and \code{time_period} (time period).}

\item{freq}{A character string indicating the frequency of the data ("Q" for quarterly, "A" for annual). Defaults to "Q".}
}
\value{
A data frame with an additional \code{obs_value} column, where the original values have been converted to Euro.
}
\description{
Converts data in national currencies to Euro using exchange rates from Eurostat.
}
\examples{
\dontrun{
# Example usage with quarterly data
data_eur <- nfsa_to_eur(data = your_data, freq = "Q")

# Example usage with annual data
data_eur <- nfsa_to_eur(data = your_data, freq = "A")
}
}
