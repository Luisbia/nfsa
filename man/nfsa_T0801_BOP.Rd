% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nfsa_T0801_BOP.R
\name{nfsa_T0801_BOP}
\alias{nfsa_T0801_BOP}
\title{Compare NFSA T0801 data with BOP data and identify discrepancies.}
\usage{
nfsa_T0801_BOP(
  country,
  quarter,
  threshold = 5,
  input_sel = here::here("data", "q", "new", "nsa"),
  output_sel = here::here("output", "inter_domain")
)
}
\arguments{
\item{country}{A character vector specifying the country code (e.g., "AT").}

\item{quarter}{A character vector specifying the quarter of the data (e.g., "2025Q2").}

\item{threshold}{A numeric value specifying the threshold for the difference
between NFSA and BOP data.  Differences exceeding this threshold are flagged.
Default is 5.}

\item{input_sel}{A character string specifying the directory containing the input
NFSA data. Default is \code{here::here("data", "q", "new", "nsa")}.}

\item{output_sel}{A character string specifying the directory where the output
Excel file will be saved. Default is \code{here::here("output", "inter_domain")}.}
}
\value{
This function does not return a value. It generates an Excel file
containing the comparison results in the specified output directory.  A
console message indicates the file creation. If no inconsistencies are found,
a success message is printed.
}
\description{
This function compares NFSA T0801 data with Balance of Payments (BOP) data,
identifies discrepancies exceeding a specified threshold, and outputs the results
to an Excel file.
}
\details{
The function reads NFSA data using \code{nfsa_get_data} and BOP data from
XML files in a specified directory. It joins these datasets, calculates the
difference between NFSA and BOP values, and filters for differences exceeding
the provided threshold. The results are then written to an Excel file.
}
\examples{
\dontrun{
# Example usage:
nfsa_T0801_BOP(country = "AT", quarter = "2024Q2", threshold = 10)
}

}
