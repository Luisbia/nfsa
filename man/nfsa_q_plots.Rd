% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nfsa_q_plots.R
\name{nfsa_q_plots}
\alias{nfsa_q_plots}
\title{Generate quarterly plots comparing NFSA data}
\usage{
nfsa_q_plots(
  country,
  comparison = c("adjusted_unadjusted", "new_prev_unadjusted", "new_prev_adjusted"),
  output_sel = here::here("output", "plots"),
  time_min = "1999-Q1",
  my_theme = ggthemes::theme_fivethirtyeight(),
  my_colours = c("#B656BD", "#208486"),
  ...
)
}
\arguments{
\item{country}{A character string specifying the country code (e.g., "AT").}

\item{comparison}{A character string specifying the type of comparison:
\itemize{
\item "adjusted_unadjusted": Compare adjusted (Y) vs unadjusted (N) from new version
\item "new_prev_unadjusted": Compare new vs prev versions of unadjusted data (T0801)
\item "new_prev_adjusted": Compare new vs prev versions of seasonally adjusted data (T0801SA)
}}

\item{output_sel}{A character string specifying the path to the directory where the generated
PDF file should be saved. Defaults to \code{here("output", "plots")}.}

\item{time_min}{A character string specifying the earliest time period to include in the plots,
in the format "YYYY-QX" (e.g., "1999-Q1"). Defaults to "1999-Q1".}

\item{my_theme}{A ggplot2 theme object to use for the plots.
Defaults to \code{ggthemes::theme_fivethirtyeight()}.}

\item{my_colours}{A character vector of two colors for the lines.
Defaults to c("#B656BD","#208486").}

\item{...}{Additional filter arguments passed to \code{nfsa_get_data()}. Can be one or more filter expressions
applied to columns in the original parquet files (e.g., \code{filters = "ref_sector == 'S13'"}).}
}
\value{
None. The function generates and saves a PDF file containing the plots.
}
\description{
This function creates time series plots comparing different versions and adjustments of NFSA quarterly data.
It supports three comparison modes:
\itemize{
\item Comparing adjusted vs unadjusted data from the same version
\item Comparing new vs previous versions of unadjusted data
\item Comparing new vs previous versions of seasonally adjusted data
}
}
\examples{
\dontrun{
# Compare adjusted vs unadjusted
nfsa_q_plots(country = "AT", comparison = "adjusted_unadjusted")

# Compare new vs previous unadjusted
nfsa_q_plots(country = "AT", comparison = "new_prev_unadjusted")

# Compare new vs previous adjusted
nfsa_q_plots(country = "AT", comparison = "new_prev_adjusted")
}

}
